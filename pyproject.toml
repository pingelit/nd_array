[project]
name = "nd-array-docs"
version = "0.1.0"
description = "Documentation tooling for nd_array."
requires-python = ">=3.10"
dependencies = [
  "mkdocs>=1.5",
  "mkdocs-material>=9.5",
  "mkdoxy>=1.2.8",
]
